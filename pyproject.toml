[project]
name = "agent-auditor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hajuanas92-code", email = "hajuanas92@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "google-cloud-logging>=3.16.3",
    "groq>=1.7.0",
    "pygithub>=2.10.0",
    "python-dotenv>=1.2.3",
    "streamlit>=1.63.0",
]

[project.scripts]
agent-auditor = "agent_auditor:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
